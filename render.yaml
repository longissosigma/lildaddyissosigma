services:
  - type: web
    name: sonyouknowwhat
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dadweb:app --bind 0.0.0.0:$PORT
    plan: free
    pythonVersion: 3.11.9